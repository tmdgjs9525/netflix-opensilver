<UserControl
    x:Class="netflix_opensilver.Views.Main.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:netflix_opensilver.Views.Main">

    <ScrollViewer Background="{StaticResource MainBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="68" />
                <RowDefinition Height="950" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <local:RecommendedVideo Grid.Row="0" Grid.RowSpan="2" />

            <local:TopMenu Grid.Row="0" />

            <TextBlock Grid.Row="2" Text="{Binding Test.Name}" />
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Test.RecommendList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image
                            Margin="10,0,0,0"
                            Cursor="Hand"
                            Source="{Binding PosterUrl}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>

</UserControl>
