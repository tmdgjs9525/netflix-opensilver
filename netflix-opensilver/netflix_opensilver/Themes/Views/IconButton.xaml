<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:units="clr-namespace:netflix_opensilver.Themes.Units">
    <Style TargetType="units:IconButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="units:IconButton">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        CornerRadius="{TemplateBinding CornerRadius}">
                        <StackPanel Margin="5,2,10,2" Orientation="Horizontal" HorizontalAlignment="Center">
                            <Viewbox Width="{TemplateBinding IconSize}" Height="{TemplateBinding IconSize}">
                                <Path Data="{TemplateBinding Icon}" Fill="{TemplateBinding Fill}" />
                            </Viewbox>
                            <ContentPresenter
                                Margin="10,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
